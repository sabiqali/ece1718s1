To run the module:
1. run the command "make" to create the .ko file
2. execute the command "insmod key_sw.ko" to insert the driver into the kernel module

To remove the driver:
1. execute the command "rmmond key_sw.ko" to remove the driver from the kernel module