To run the module:
1. run the command "make" to create the .ko file
2. execute the command "insmod ledr_hex.ko" to insert the driver into the kernel module

To remove the driver:
1. execute the command "rmmond ledr_hex.ko" to remove the driver from the kernel module